import { Text, View } from "react-native";

export default function ProfileScreen() {
  return(
    <View>
      <Text style={{textAlign: "center"}}>
        INI PROFILE
      </Text>
    </View>
  )
}